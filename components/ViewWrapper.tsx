import styled from "styled-components";
import Scroll from "./Scroll";

const ViewWrapper = styled(Scroll)`
  flex-direction: row;
  justify-content: space-evenly;
  align-items: stretch;
`;

export default ViewWrapper;
