import styled from "styled-components";

// Stack is already taken as a page
const Pile = styled.div`
  display: flex;
  flex-direction: column;
  padding: 0.2rem;
  justify-content: space-evenly;
`;

export default Pile;
